---
# Dummy integration test for the win_scom_dummy_alert target.
# This test exists purely to validate that the CI integration target detection
# mechanism (scripts/get_integration_targets.sh) correctly maps BOTH
# dummy_alert.ps1/yml AND dummy_alert_info.ps1/yml to this single target.

- name: "Testing win_scom_dummy_alert"
  ansible.builtin.debug:
    msg: "Testing win_scom_dummy_alert"

- name: "win_scom_dummy_alert | Confirm both modules map to this target"
  ansible.builtin.debug:
    msg: >-
      Modules sandbox.ci.dummy_alert and sandbox.ci.dummy_alert_info
      both resolve to the win_scom_dummy_alert integration target.
